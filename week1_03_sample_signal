import matplotlib.pyplot as plt

# Define the start and end samples for the segment you want to view
start_sample = 0
end_sample = 200  # Further reduced to view an even smaller segment

# Assuming it's an ECG signal and we want to plot the first lead
# You might need to adjust '0' if your signal has multiple leads and you want a different one
plt.figure(figsize=(15, 6))
plt.plot(record.p_signal[start_sample:end_sample, 0])
plt.title(f'Signal from {record_name} (Lead {record.sig_name[0]}) - Samples {start_sample} to {end_sample}')
plt.xlabel('Sample Number')
plt.ylabel(f'Amplitude ({record.units[0]})')
plt.grid(True)
plt.show()
