# Get the segment of the signal we were working with
signal_segment = record.p_signal[start_sample:end_sample, 0]

# Reverse the signal segment
reversed_signal_segment = signal_segment[::-1]

plt.figure(figsize=(15, 6))
plt.plot(reversed_signal_segment)
plt.title(f'Reversed Signal Segment from {record_name} (Lead {record.sig_name[0]}) - Samples {start_sample} to {end_sample}')
plt.xlabel('Reversed Sample Index')
plt.ylabel(f'Amplitude ({record.units[0]})')
plt.grid(True)
plt.show()
